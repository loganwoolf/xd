{
	"name": "xd",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"devDependencies": {
		"@biomejs/biome": "^2.1.3",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"file-icons": "^0.0.0",
		"ink": "^6.1.0",
		"react": "^19.1.1"
	},
	"scripts": {
		"start": "bun run index.ts",
		"dev": "bun run index.ts",
		"build": "bun build --compile --outfile xd index.ts",
		"lint": "biome lint .",
		"format": "biome format . --write",
		"check": "biome check .",
		"check:fix": "biome check . --apply"
	}
}
